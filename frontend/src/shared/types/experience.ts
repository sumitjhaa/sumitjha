export interface CompanyData {
    name: string
    logo?: string
    location: string
    role: string
    period: string
}

export interface ProjectData {
    title: string
    highlight: string
    description: string
    points: string[]
}
